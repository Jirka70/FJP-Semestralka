package org.example.semantic.symbolTable.descriptor;

public enum DescriptorType {
    CLASS_DESCRIPTOR, METHOD_DESCRIPTOR, APP_DESCRIPTOR, FOR_LOOP, WHILE_LOOP, IF, ELSE, DO_WHILE, CASE, SWITCH, VARIABLE_DESCRIPTOR
}
